import { createTheme } from "@mui/material"
const theme = createTheme({
  palette: {
    primary: {
      main: "#0172B6",
      dark: "#212121",
      light: "#FAFAFA",
      contrastText: "#FAFAFA"
    },
    secondary: {
      main: "#03B6F0",
      dark: "#111111",
      light: "#FFFFFF",
      contrastText: "#FAFAFA"
    }
  }
})
export default theme
